<{{ tag_type }} class="container-module {{ container_type }} {{ class|default('') }}" {{ (id is not empty)? 'id='~ id|default('')}}  style="{{ bg_color|default(false) ? 'background-color:'~bg_color : '' }}">
{% for content in contents %}
    {{ content|raw }}
{% endfor %}
</{{ tag_type }}>
