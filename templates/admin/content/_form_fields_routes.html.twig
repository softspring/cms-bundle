{% trans_default_domain 'sfs_cms_contents' %}

<section class="mb-5" aria-labelledby="content-routes-fields-title">
    <h2 class="h5 border-bottom pb-2 mb-4" id="content-routes-fields-title">{{ 'admin_content.form.sections.routes.title'|trans }}</h2>

    {{ form_errors(form.routes) }}
    {{ form_widget(form.routes) }}
</section>
