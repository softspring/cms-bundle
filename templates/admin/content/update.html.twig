{% trans_default_domain 'sfs_cms_contents' %}

{% extends '@SfsCms/admin_layout.html.twig' %}

{% block title %}{{ ('admin_'~content~'.update.meta.title')|trans({'%name%':entity.name}) }}{% endblock %}

{% form_theme form '@SfsPolymorphicFormType/polymorphic-form-theme.html.twig' '@SfsCms/cms_form_theme.html.twig' %}

{% block breadcrums_content %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ url('sfs_cms_admin_content_'~content~'_list') }}">{{ ('admin_'~content~'.list.breadcrumb')|trans }}</a></li>
    <li class="breadcrumb-item active" aria-current="content">{{ ('admin_'~content~'.update.breadcrumb')|trans }}</li>
{% endblock breadcrums_content %}

{% block content %}
    <h1 class="h3">{{ ('admin_'~content~'.update.title')|trans }}</h1>

    {% include '@SfsCms/admin/content/_content_tabs.html.twig' with {'current':'update', 'content':content, 'entity':entity} %}

    {{ form_start(form) }}

    <div class="tab-content" id="myTabContent">
        <div class="tab-pane fade show active" id="edit" role="tabpanel" aria-labelledby="edit-tab">
            <div class="container pt-3 pb-3">
                {{ form_row(form.name) }}
{#                {{ form_row(form.layout) }}#}
                {{ form_rest(form) }}
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-end align-items-center">
        <a href="{{ url('sfs_cms_admin_content_'~content~'_list') }}" class="btn btn-outline-secondary">{{ ('admin_'~content~'.update.actions.cancel.button')|trans }}</a>
        <input type="submit" value="{{ ('admin_'~content~'.update.actions.update.button')|trans }}" class="btn btn-primary ms-3" />
    </div>
    {{ form_end(form) }}
{% endblock content %}